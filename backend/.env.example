# Proměnné pro lokální vývoj backendu
# Zkopíruj tento soubor jako .env a doplň hodnoty

# Připojení k databázi
# MOŽNOST A: Lokální PostgreSQL (doporučeno)
DATABASE_URL="postgresql://postgres:password@localhost:5432/engineers_db"

# MOŽNOST B: Sdílená Render databáze (bez nutnosti lokálního Postgresu)
# DATABASE_URL="postgresql://engineers_db_hqyh_user:HESLO@dpg-d8f9qjmq1p3s73do8g7g-a/engineers_db_hqyh"

# Tajný klíč pro session (cokoliv náhodného)
SESSION_SECRET="muj-lokalni-tajny-klic-123"

# Render nastaví automaticky, lokálně nepotřebné
# NODE_ENV="development"
# CORS_ORIGIN="http://localhost:5173"
